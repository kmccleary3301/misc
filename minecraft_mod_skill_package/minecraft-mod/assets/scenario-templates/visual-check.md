# Visual check

## Goal
Capture repeatable visual proof for a changed surface such as a GUI, renderer, or HUD.

## Requirements
- stable world or scene
- stable resolution / GUI scale if possible
- paired metadata for each screenshot
- a structured state dump when feasible

## Expected artifacts
- `screenshots/<name>.png`
- `screenshots/<name>.json`
- `world-state.json` or equivalent
